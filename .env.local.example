# Local Docker Compose values only. Copy to .env.local before running Compose.
# Never place a Vercel, Neon, Blob, or other production secret in this file.

LOCAL_POSTGRES_DB=portfolio_dev
LOCAL_POSTGRES_USER=portfolio
LOCAL_POSTGRES_PASSWORD=local-development-only
LOCAL_POSTGRES_PORT=5432
APP_PORT=3000
